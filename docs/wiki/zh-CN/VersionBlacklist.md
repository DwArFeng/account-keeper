# Version Blacklist - 版本黑名单

## 说明

本项目的版本黑名单，列出了本项目的版本黑名单，请注意避免使用这些版本。

列入黑名单的版本，可能是因为以下原因：

- 该版本存在严重的 Bug，可能会导致核心功能无法正常使用。
- 该版本存在严重的 Bug，可能会导致数据丢失、数据错误等严重后果。

## 设计目的

该黑名单旨在帮助开发人员快速识别各版本中已知的问题点，明确受影响的模块与典型触发场景，从而做出基于事实的用/避决策与迁移安排，
而不是简单地“一刀切”禁用某一版本。

## 如何使用

黑名单不是绝对禁用清单。遇到被列入黑名单的版本时，应当基于"问题原因—是否命中—影响面"的原则进行研判：

- 是否直接或间接使用了被提及的模块/类（例如：`com.dwarfeng.acckeeper.impl.handler.PurgeProcessor`）。
- 业务中是否存在与“原因”描述相符的顺序敏感或易触发场景（例如：清除任务执行失败时的事件推送场景）。
- 是否已出现与问题相符的日志告警、数据核对失败或线上异常案例。

若未使用黑名单中提及的问题代码部分，或经过验证不命中触发条件，则仍然可以继续使用该版本；若命中或存在较大不确定性，
建议参考对应编号“详细原因”章节末尾的迁移建议执行升级或规避方案，并充分做回归与灰度验证。

请注意：迁移建议仅针对对应编号的问题，所推荐版本可能仍存在其他风险。请继续核查该推荐版本是否仍在黑名单中；
若仍在黑名单中，请持续研判并迭代升级，直至风险可接受或升级至未被列入黑名单的版本。

## 版本黑名单

| 编号                                           | 大版本   | 起始版本    | 结束版本    | 原因                  |
|----------------------------------------------|-------|---------|---------|---------------------|
| [BLACKLIST-20250810.1](#BLACKLIST-202508101) | 1.9.x | 1.9.0.a | 1.9.0.a | 清除任务执行失败时，事件推送行为不正常 |

## 详细原因

### BLACKLIST-20250810.1

原因：清除机制存在 bug，清除任务执行失败时，事件推送行为不正常。

- 受影响模块/类：
  - `com.dwarfeng.acckeeper.impl.handler.PurgeProcessor`。
  - `com.dwarfeng.acckeeper.stack.handler.PushHandler`。
- 典型触发条件：
  - 清除任务执行失败时。
  - 侦听 `PushHandler.purgeFailed` 事件的程序需要执行相关业务逻辑调度。
- 典型症状：
  - `PushHandler.purgeFailed` 事件无法正常触发。
  - 侦听该事件的其它程序无法执行相关的业务逻辑调度。
- 影响范围：
  - 直接或间接依赖清除机制、并需要侦听清除失败事件的上层功能与接口。

迁移建议：升级至 1.9.0.b 及以上版本。

## 注意事项

- 黑名单用于风险提示，并非强制禁用。是否升级或规避，应以是否命中“详细原因”所述的模块与场景为依据。
- 建议遵循生产变更流程：评估 -> 测试 -> 灰度 -> 观测 -> 全量，过程中启用充分的日志与指标监控。
- 如发现新问题或修复版本，请同步更新本黑名单与变更记录，保持信息一致性。
